---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
const posts = (await getCollection('blog')).sort((a,b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---
<BaseLayout title="Nagpur Mail | Real-Time Updates" description="Latest news from Nagpur city.">
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-10">
        <div class="lg:col-span-8 space-y-6">
            {posts.map(post => (
                <article class="bg-white p-4 border border-gray-200 hover:border-blue-600 transition-all flex flex-col md:flex-row gap-6 shadow-sm">
                    <div class="md:w-1/3">
                        <img src={post.data.image} class="aspect-[4/3] object-cover rounded-sm w-full" />
                    </div>
                    <div class="md:w-2/3">
                        <h2 class="text-[18px] md:text-[22px] font-black text-[#002b5c] leading-tight mb-2 uppercase tracking-tighter italic">
                            <a href={`/posts/${post.slug}`}>{post.data.title}</a>
                        </h2>
                        <div class="flex items-center gap-2 text-[10px] text-gray-400 font-bold uppercase mb-3">
                            <span>By <span class="text-blue-700">{post.data.author}</span></span>
                            <span>•</span>
                            <time>{post.data.pubDate.toDateString()}</time>
                        </div>
                        <p class="text-gray-600 text-[13px] leading-relaxed line-clamp-2">{post.data.description}</p>
                    </div>
                </article>
            ))}
            
            <button class="w-full py-2 bg-[#4285F4] text-white text-[11px] font-black uppercase italic rounded-sm">+ Load More</button>
        </div>

        <div class="lg:col-span-4">
            <Sidebar />
        </div>
    </div>
</BaseLayout>
